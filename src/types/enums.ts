export enum ActiveStatus {
    true = 1,
    false = 0
}

export enum GatewayType {
    SIP = "SIP",
    SMPP = "SMPP",
    SMTP = "SMTP"
}

export enum Role {
    user = "user",
    admin = "admin",
}


export enum Quality{
    super = "super",
    fine = "fine",
    standard = "standard"
}


